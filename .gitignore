# python
__pycache__/
*.py[cod]
.venv/
venv/

# node
node_modules/
.next/
next-env.d.ts
.turbo/

# secrets and collected data. Note the leading slash: this must NOT match
# dashboard/public/data/, which holds the results the dashboard renders and
# therefore has to be committed.
/data/
.env
.env.local

# generated figures. lab.json IS committed so the dashboard works on clone.
outputs/*.png

# auto-generated by `next dev`, not part of the deliverable
dashboard/AGENTS.md
dashboard/CLAUDE.md

# local only -- interview prep notes and the email draft
docs/interview-defense.md
docs/email.md

# NOTE: GitHub Pages publishes main:/docs. Anything committed under docs/
# becomes publicly readable. Add private notes to the ignore list ABOVE
# before creating them.
